import { ReactNode } from 'react';
import styles from './layout.module.scss';

const Footer = () => {
  return (
    <footer className={styles.footer}>
      <div>News Test app</div>
    </footer>
  );
};

export default Footer;
